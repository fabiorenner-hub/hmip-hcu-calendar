// The single place with project-specific values. Everything else imports from here.
export const PLUGIN_ID = 'de.fr.renner.plugin.calendar';
export const GITHUB_REPO = 'fabiorenner-hub/hmip-hcu-calendar';
/** UPPER env prefix. Env vars: CALENDAR_DATA_DIR, CALENDAR_VERSION, CALENDAR_OTA_*. */
export const ENV_PREFIX = 'CALENDAR';
/** Unique dashboard port (see the port registry in the global steering doc). */
export const DASHBOARD_PORT = 8092;
